#include "Header.h"
#include "User.h"
#include "elevator.h"

using namespace std;

void simulation(Elevator elevator, vector<User>people)
{
	bool done = false;
	while (!done)
	{
		while (elevator.getFloor() != MAXFLOOR)
		{

		}
	}
}